<!DOCTYPE html>
<html>
  <head>
    <title>Myapp</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="row">
        <div class="col-2">
          <%= link_to root_path, class: 'logo' do %>
            <div class="circle">
              <br />
              Alternative
              </br />
              Facts
            </div>
          <% end %>
        </div>
        <div class="col col-sm-3 hidden-xs-down hidden-sm-down slogan">
          When regular facts don't fit your narrative.
        </div>
        <div class="col-7 offset-2 offset-sm-0 col-sm-5 links">
          <!-- Button trigger modal -->
           <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
           &nbsp;&nbsp;
          <%= link_to 'About', about_path %>
        </div>
    </header>


    <!-- Modal -->
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
            <h4 class="modal-title" id="myModalLabel">Submit your Alternative!</h4>
          </div>
          <%= simple_form_for Quote.new do |f| %>
            <div class="modal-body">
              <%= f.input :saying, label: "Enter your quote:", placeholder: "And it was huge!" %>
              <br />
              <%= f.input :author, label: "Tell us who said it:", placeholder: "Donald Trump" %>
              <br />
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <%= f.submit 'Create', class: 'btn btn-outline-danger' %>
          </div>
          <% end %>
        </div>
      </div>
    </div>


<hr />

    <br class="clear" />
    
    <div class="container">
      <% if flash[:error].present? %>
        <div class="col-10 offset-1 alert alert-danger">
          <%= flash[:error].html_safe %>
        </div>
      <% end %>
    <%= yield %>
    </div>
    
    <div class="footer">
      <div class="container">
        <div class="row">
          <div class="col-12 col-sm-4"> 
            <%= link_to "Get more Alternative Facts!", root_path, class: 'next-quote' %>
          </div>
          <div class="col-12 col-sm-8 footer-blurb">
            Coded by Ursula MV with all due respect for the 45th president of the United States of America
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
